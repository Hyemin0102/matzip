import React from 'react';
import {SafeAreaView, StyleSheet, Text, View} from 'react-native';

const SearchLocationScreen = () => {
  return (
    <SafeAreaView>
      <Text>SearchLocation 스크린</Text>
    </SafeAreaView>
  );
};

export default SearchLocationScreen;
